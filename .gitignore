# Local tool settings
.claude/settings.local.json

# Dependencies and OS clutter
node_modules/
.DS_Store
Thumbs.db

# Raw clips from Suno; tools/build_sounds.py turns them into the game sounds in sounds/
sounds/originals/

# Raw asset packs; only the models the game loads are committed (see models/)
models/originals/
